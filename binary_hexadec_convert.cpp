//convert binary to hexadecimal

#include <bits/stdc++.h>

using namespace std;

string binary;
int convert();
int binary
string hex = "0x";
int main() {
  cin>>binary;
}

int convert(binary) {
  for (int i = 1; i <binary.length()/4 +1; i++) {
    string a = binary.substr(i, a)
  }
  //convert sets of 4 binary to hexa by matching, string hex+= hexa
}
int i =1;
while (i <binary.length()/4 +1) {
  int b = 0;
  string a = binary.substr(b, 4)
  b = i*4 +1;
  if (a == "")
  i++;
}
